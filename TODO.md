# TODO
- [ ] Update CSP in `frontend/next.config.ts` to allow `data:` in `font-src` so embedded/base64 fonts are permitted in production.
- [ ] Rebuild and redeploy frontend.
- [ ] Verify browser console has no CSP font errors.

